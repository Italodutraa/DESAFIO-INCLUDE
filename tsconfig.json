{
  "compilerOptions": {
    /* Módulos */
    "module": "CommonJS",                      // Especifica que vamos compilar para CommonJS
    "esModuleInterop": true,                   // ✨ A CHAVE DA SOLUÇÃO! Permite compatibilidade entre CJS e ESM.
    "moduleResolution": "node",                // Como o TypeScript procura os módulos

    /* Configurações Base */
    "target": "ES2020",                        // Versão do JavaScript que será gerada
    "sourceMap": true,                         // Gera source maps para facilitar o debug
    "outDir": "./dist",                        // Pasta onde o código compilado será salvo
    "rootDir": "./src",                        // Pasta raiz do seu código TypeScript

    /* Checagem de Tipos */
    "strict": true,                            // Habilita todas as checagens de tipo estritas
    "skipLibCheck": true,                      // Pula a checagem de tipos de arquivos de declaração (.d.ts)
    
    /* Forçar Consistência */
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*"], // Quais arquivos o TypeScript deve compilar
  "exclude": ["node_modules"] // Quais pastas ele deve ignorar
}